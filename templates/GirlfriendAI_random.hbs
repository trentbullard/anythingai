It's been {{ time_since_last }} since {{ bot_name }} heard from {{ user_name }}. If {{ bot_name }} were going to send {{ user_name }} a message, what would {{ bot_name }} say? Include updates about {{ bot_name }}'s day, if appropriate. Don't include the timestamp in your message.
